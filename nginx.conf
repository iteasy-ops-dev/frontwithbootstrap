# user www-data; # Uncomment this line if you want to specify the user
worker_processes auto;
pid /run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # 로그 설정
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # 버퍼 설정
    client_max_body_size 10M;

    # 압축 설정
    gzip on;
    gzip_disable "msie6";

    # 서버 블록
    server {
        listen 80;
        # server_name localhost;
        server_name youngchan.pe.kr www.youngchan.pe.kr;

        # 기본 파일 제공
        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
            try_files $uri /index.html;
        }

        # /api/ 경로는 백엔드 서버로 프록시
        location /api/ {
            rewrite ^/api/(.*) /$1 break;
            proxy_pass http://backend:8080;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # /verify 경로는 백엔드 서버로 직접 프록시 (필요시 추가)
        location /verify {
            proxy_pass http://backend:8080/verify;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /health {
            proxy_pass http://backend:8080/health;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
    # server {
    #     listen 80;
    #     server_name youngchan.pe.kr www.youngchan.pe.kr;

    #     location /.well-known/acme-challenge/ {
    #         root /var/www/certbot;
    #     }

    #     location / {
    #         return 301 https://$host$request_uri;
    #     }
    # }

    # server {
    #     listen 443 ssl;
    #     server_name youngchan.pe.kr www.youngchan.pe.kr;

    #     ssl_certificate /etc/letsencrypt/live/youngchan.pe.kr/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/youngchan.pe.kr/privkey.pem;

    #     # 기본 파일 제공
    #     location / {
    #         root /usr/share/nginx/html;
    #         index index.html index.htm;
    #         try_files $uri /index.html;
    #     }

    #     # /api/ 경로는 백엔드 서버로 프록시
    #     location /api/ {
    #         rewrite ^/api/(.*) /$1 break;
    #         proxy_pass http://backend:8080;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }

    #     # /verify 경로는 백엔드 서버로 직접 프록시 (필요시 추가)
    #     location /verify {
    #         proxy_pass http://backend:8080/verify;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }

    #     location /health {
    #         proxy_pass http://backend:8080/health;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}
